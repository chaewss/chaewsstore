spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/chaewsstoretest
    username: sa

  jpa:
    hibernate:
      ddl-auto: none
jwt:
  header: Authorization
  secret: ${SECRET_KEY}
